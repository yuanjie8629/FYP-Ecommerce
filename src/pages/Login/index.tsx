import Layout from '@components/Layout';
import LoginModal from '@components/Modal/LoginRegModal/LoginModal';

const Login = () => {
  return (
    <Layout>
      <LoginModal />
    </Layout>
  );
};

export default Login;
